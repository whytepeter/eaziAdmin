query GetCompanyReconciliationBalances($input: GetCompanyReconciliationBalancesInput) {
  GetCompanyReconciliationBalances(input: $input) {
    data {
      inflow
      outflow
      walletBalance
      reconciliationValue
    }
  }
}
