mutation ConfirmLoanPayment($loanId: String!, $amount: Float!, $photoUrl: String!) {
  ConfirmLoanPayment(loanId: $loanId, amount: $amount, photoUrl: $photoUrl) {
    success
    message
    data
  }
}
